import 'package:flutter/material.dart';

class HomePageDemo extends StatelessWidget {
  const HomePageDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Home Pageee")),
    );
  }
}
